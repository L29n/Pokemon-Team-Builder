name: Continuous Deployment

on:
  push:
    branches:
    - "Mike's-Frontend"
jobs:
  push_to_registry:
    name: Deploying Docker Image to Container
    runs-on: ubuntu-latest
    steps:
      - name: Pull Latest Code From Github Repo
        uses: actions/checkout@v3

    